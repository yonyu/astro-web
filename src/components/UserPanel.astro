---
const { userName, isAdminUser } = Astro.props;
---

<p>Welcome {userName}</p>
{
    isAdminUser && (
        <section class="admin-panel">
        <h3>Admin control panel</h3>
        <p>Admin functionality goes here</p>
        </section>
    )
}